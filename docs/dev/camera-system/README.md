# 相机系统开发文档

## 目标

只保留 orbit 相机，让用户始终可以围绕核心车辆自由旋转、缩放和平移视角，同时让车辆与相机在固定步渲染插值下保持稳定的相对位置。

## 当前操作逻辑

| 输入 | 行为 |
|------|------|
| 鼠标左键拖拽 | 围绕飞车自由旋转视角 |
| 鼠标滚轮 | 缩放距离 |
| 鼠标右键拖拽 | 平移视角 |
| `R` | 重置 orbit 相机到飞车后上方 |
| `W/S` | 加速 / 减速 |
| `A/D` 或 `ArrowLeft/ArrowRight` | 左右转向并自动协调侧倾 |
| `Q/E` | 手动左右侧倾，不改变航向 |
| `ArrowUp / ArrowDown` | 爬升 / 俯冲，松键自动回平 |
| `Shift` | 加力 |

## 本次落地内容

- ⚡ 2026-08-04 已安装 `camera-controls@3.1.2`，删除 `OrbitControls` import，并将 `OrbitCameraRig` 收敛为 `VehicleCameraRig`。
- ⚡ 2026-08-04 将迁移时沿用的 `0.58` 水平/垂直旋转倍率提高到 `1.6`，增强鼠标拖拽响应，不改变跟随、缩放或阻尼时间。
- ⚡ 迁移保留拖拽 orbit、滚轮缩放、右键平移、速度 FOV、航向跟随和 `R` 重置行为；位置前视在抖动修复中移除。
- 🆕 自动跟随只通过 camera-controls 的公开 position / target API同步，主循环仍负责每帧传入 `dt`。

- ⚡ 删除旧跟随相机和所有相机模式切换逻辑。
- ⚡ 删除页面相机模式按钮；核心驾驶收缩后 HUD 也已完全移除。
- ⚡ `Experience` 固定使用 `VehicleCameraRig`。
- ⚡ `KeyboardPointerInput` 固定禁用鼠标飞控输入，鼠标只交给 camera-controls。
- ⚡ `R` 保留为 orbit 相机重置快捷键。
- ⚡ 落日主题将 target offset 从 `0.8` 降到 `0.35`，initial offset 从 `(0, 4, 12)` 调整为 `(0, 2.7, 13.5)`。
- ⚡ 自动位置跟随改为复用相邻两帧 interpolated render state 的位移，不再增加位置阻尼或速度方向前视。
- 🆕 相机 FOV 根据 `72–260` 速度区间从 `64°` 平滑扩展到 `74°`，FOV sharpness 为 `4.8`。
- 🆕 相机反馈状态已加入 `Experience` 诊断快照。
- 🆕 已加入 sharpness `7.5` 的水平航向跟随：自动旋转相机相对 target 的偏移，保留用户 orbit 角度；`R` 按当前航向回到车尾后上方。
- ⚡ 2026-08-04 将 initial offset 从 `(0, 2.7, 13.5)` 收近为 `(0, 2.25, 9.5)`，适配只有车辆的核心画面。

## 修复记录

- ⚡ 修复模式切换和按钮长期干扰问题：彻底移除模式切换，避免 UI 状态、快捷键和相机 rig 之间的状态不同步。
- ⚡ camera-controls 每帧先消费用户输入，再由 rig 读取当前 position/target，并对两者应用相同的载具跟随位移和航向旋转。
- 🆕 首轮迁移遗漏 position 同步，车辆前进时观察距离从 `9.76` 漂移到 `92`；已修复为 position/target 同量平移。
- ⚡ 固定步进首轮实现中，车辆直接应用插值姿态，相机位置仍经过独立弹簧；可变 `alpha` 下两者响应不一致，已改为共享 render-position delta。
- ⚡ `setLookAt(..., false)` 只更新 camera-controls 内部状态，真实 camera 曾延迟一帧写入；已在每次 setLookAt 后调用 `update(0)` 同帧提交。
- 🆕 新构图下太阳的首屏归一化位置约为 `(0.664, 0.828)`，同时 AE86 仍位于相机中心区域。
- ⚡ 位置跟随不再允许自动目标滞后；速度反馈只保留 FOV，重置后恢复 `64°`。
- 🆕 连续 `90°` 转向时相机保留约 `3.75°` 航向滞后，停转 1 秒后误差收敛到约 `0.002°`，观察半径误差小于浮点精度。
- 🆕 航向跨越 `+179° -> -179°` 时单帧剩余误差约 `1.77°`，确认使用最短角度路径。
- ⚡ 左转 `90°` 后执行 `R`，当前默认相机偏移应为约 `(9.5, 2.25, 0)`，位于当前车尾而非世界固定方向。
- 🆕 核心画面已在桌面 `1280 x 720` 与移动 `390 x 844` 复验，车辆保持完整且没有裁切。

## 验证项

- `npm run build` 通过。
- 源码和构建依赖中不再出现 Three examples `OrbitControls`。
- camera-controls 实例只创建一次并在 `Experience.stop()` 中释放。
- 页面不再显示相机模式按钮。
- 鼠标可以自由旋转、缩放和平移视角。
- 键盘仍可控制飞车。
- `R` 可以重置 orbit 相机。
- 加速和减速期间相机与车辆保持固定相对位置，FOV 平滑变化且不产生非有限数值。
- 转向期间相机保持用户选定的相对 orbit 构图，停止转向后平滑收敛到当前航向。

## camera-controls 验证记录

- 🆕 `npm run build` 通过，源码中不再存在 `OrbitControls` / `OrbitCameraRig` 引用。
- 🆕 车辆前进约 `86` 单位后 camera-controls 距离保持 `9.7628`，漂移为 `0`。
- 🆕 鼠标 orbit 改变相机位置但距离只产生浮点误差；滚轮将距离从 `9.76` 调整到 `5.98`。
- 🆕 `R` 重置后距离精确恢复 `9.7628`；模型状态 `ready`、`glError = 0`，无浏览器异常事件。
- 🆕 入口包由约 `644.50 kB` 增至 `671.51 kB`，新增约 `27 kB` 为 camera-controls 运行时代码。
- ⚡ 固定步进落地后，camera-controls 只消费 previous/current 插值得到的 render state；`R` 也以该状态重置。
- ⚡ 修复前采样中 camera-controls 内部距离恒为 `9.7628`，但真实 camera 到车辆的距离范围达 `1.60`，确认存在一帧提交延迟。
- 🆕 同帧提交修复后，在 `15.6–34.3ms`、`0/1/2` 子步混合的 90 个样本中，相机到车辆距离与观察深度 range 均为 `0`，`glError = 0`。
- 🆕 `W+A` 联合输入的 100 个样本覆盖 `0/1/2/3` 子步与接近完整的 `alpha` 区间，相机距离和观察深度波动仅约 `1e-13` 浮点误差。
- 🆕 鼠标 orbit 后 camera-controls 距离保持 `9.7628`，`R` 可恢复默认构图；桌面 `1280 x 720` 与移动 `390 x 844` 均完整显示车辆且 `glError = 0`。
- ⚡ 固定 `150px` 横向拖拽的实际旋转角随倍率 `0.58 -> 1.0 -> 1.6` 从 `33.24° -> 57.31° -> 91.69°`；最终拖拽期间相机距离 range 为 `0`，同帧跟随修复未回归。
